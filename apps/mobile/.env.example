# Local dev values. EXPO_PUBLIC_ prefix is required for client-side access.
# For builds, these are set per-profile in eas.json instead.

# Where the Next.js API is reachable from the device/simulator.
# Use your machine's LAN IP (not localhost) when testing on a physical device.
EXPO_PUBLIC_API_URL="http://localhost:3000"
