# React Native / Expo packages declare strict peer ranges that npm 7+ enforces
# too aggressively across a hoisted workspace tree. legacy-peer-deps lets the
# install resolve the way Expo expects (the same posture the Expo CLI uses).
legacy-peer-deps=true
